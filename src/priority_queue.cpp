#include <cstdio>

#define MAX_PRIORITY_QUEUE 100000
typedef int T;

typedef struct MA_PRIORITY_QUEUE {
    T data[MAX_PRIORITY_QUEUE];
    size_t size;
} priority_queue;

/// TODO: create necessary methods

int ma_test() {
    priority_queue p_queue;
    p_queue.size = 0;

    /// TODO: command parser
}

